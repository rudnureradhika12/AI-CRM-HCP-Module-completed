version: '3.8'
services:
  backend:
    build: ../backend
    environment:
      - DATABASE_URL=sqlite:///./ai_crm.db
      - GROQ_API_KEY=${GROQ_API_KEY}
    ports:
      - '8000:8000'
  frontend:
    build: ../frontend
    ports:
      - '3000:3000'
    depends_on:
      - backend
